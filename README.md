# AI_Voice_Assistant
AI Voice Assistant is a Python application that lets users interact with a computer using voice commands. It listens to speech, converts it to text using SpeechRecognition, processes the request, and responds with voice using pyttsx3. It can perform tasks like telling the time, answering queries, and searching Wikipedia. 🎤🤖
